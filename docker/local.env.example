# Optional provider keys for local docker compose (docker-compose.local.yml).
# Copy to docker/local.env and fill in what you need:
#   cp docker/local.env.example docker/local.env
#
# UI login still works with no keys (admin / sk-1234). Completions need at least one.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
